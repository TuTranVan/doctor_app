<%= form_for [:admin, @major] do |f| %>
  <%= render "shared/error_messages", object: @major%>

  <div class="form-group">
    <%= f.text_field :name, class: "form-control", placeholder: "Tên chuyên khoa" %>
  </div>

  <div class="form-group">
    <%= f.submit yield(:button_text), class: "btn btn-primary" %>
  </div>
<% end %>